% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_custom_filters.R
\name{apply_custom_filters}
\alias{apply_custom_filters}
\title{Apply custom filters to a dataframe}
\usage{
apply_custom_filters(
  data,
  year_col = "year",
  year_filter = NULL,
  population_type_col = "origine",
  population_type_values = NULL,
  precision_col = "precision",
  precision_filter = NULL,
  canton_col = "canton",
  canton_filter = NULL,
  other_geo_filters = list()
)
}
\arguments{
\item{data}{A dataframe}

\item{year_col}{Name of the year column (default: "year")}

\item{year_filter}{List of the form (`>`, 2000)}

\item{population_type_col}{Name of the population type column (default: "origine")}

\item{population_type_values}{Vector of values to keep for population type}

\item{precision_col}{Name of the precision column (default: "precision")}

\item{precision_filter}{List of the form (`<=`, 100)}

\item{canton_col}{Name of the canton column (default: "canton")}

\item{canton_filter}{Vector of canton values to keep}

\item{other_geo_filters}{List of other geographic filters (named list: list(commune = c("Chancy", "Dardagny")))}
}
\value{
A **list** with two elements:
\itemize{
 \item \code{data}: The filtered dataframe.
 \item \code{filters}: A list summarizing the filters applied and the reduction in rows (initial_n, final_n, removed_n).
}
}
\description{
Filters a dataframe based on various criteria (year, geography, precision, origin)
using flexible column names and relational operators.
}
\examples{
# Exemple avec colonnes standard :
apply_custom_filters(data, year_filter = list(`>`, 2010),
                     population_type_values = c("Suisse"),
                     precision_filter = list(`<=`, 100),
                     canton_filter = c("VD", "GE"))

# Exemple avec colonnes personnalisees :
apply_custom_filters(data,
                     year_col = "annee",
                     population_type_col = "type_pop",
                     canton_col = "canton_nom",
                     year_filter = list(`>`, 2010),
                     other_geo_filters = list(region = c("CH")))

}
\seealso{
\code{\link{create_clean_data}} pour la standardisation des colonnes.
}
